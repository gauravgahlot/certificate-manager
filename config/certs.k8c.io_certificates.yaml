---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: certificates.certs.k8c.io
spec:
  group: certs.k8c.io
  names:
    kind: Certificate
    listKind: CertificateList
    plural: certificates
    singular: certificate
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Certificate is the schema for the certs API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CertificateSpec defines the desired state of the Certificate.
            properties:
              altNames:
                description: Subject alternate names, other than DNSName.
                items:
                  type: string
                type: array
              dnsName:
                description: The DNS name for which the certificate should be issued.
                type: string
              organization:
                description: Name of the organization.
                type: string
              renewBeforeDays:
                description: The number of days the certificate should be renewed
                  before it expires.
                minimum: 7
                type: integer
              secretRef:
                description: A reference to the Secret object in which the certificate
                  is stored.
                properties:
                  name:
                    type: string
                required:
                - name
                type: object
              validForDays:
                default: 365
                description: The number of days until the certificate expires.
                minimum: 7
                type: integer
            required:
            - dnsName
            - organization
            - secretRef
            type: object
          status:
            description: CertificateStatus defines the observed state of the certificate.
            properties:
              state:
                description: State of the Certificate.
                enum:
                - Valid
                - Expired
                - InProgress
                type: string
            required:
            - state
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
